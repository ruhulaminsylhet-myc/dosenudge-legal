{
  "name": "taxi-platform-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-taxi \"vitest run\"",
    "test:watch": "firebase emulators:exec --only firestore --project demo-taxi \"vitest\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0",
    "vitest": "^2.1.0"
  }
}
